import React from "react";

export default function Header() {
  const [navbarOpen, setNavbarOpen] = React.useState(false);

  return <header className="text-black body-font bg-[#FFF4EC]"></header>;
}
